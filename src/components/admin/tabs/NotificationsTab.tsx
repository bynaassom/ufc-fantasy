"use client";

export default function NotificationsTab() {
  return (
    <div
      className="p-4"
      style={{
        backgroundColor: "var(--bg-elevated)",
        border: "1px solid var(--border)",
      }}
    >
      <p className="text-sm" style={{ color: "var(--text-muted)" }}>
        Gerenciamento de notificações push — em breve.
      </p>
    </div>
  );
}
