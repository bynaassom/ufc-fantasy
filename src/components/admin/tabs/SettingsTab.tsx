"use client";

export default function SettingsTab() {
  return (
    <div
      className="p-4"
      style={{
        backgroundColor: "var(--bg-elevated)",
        border: "1px solid var(--border)",
      }}
    >
      <p className="text-sm" style={{ color: "var(--text-muted)" }}>
        Configurações do app — em breve.
      </p>
    </div>
  );
}
